To run the Program code generator run the PGSMainCodeGenerator.py Python script

The FishGame and PaintGame have example games which use the Pygame Simplified API.

---
The program code generator may have some bugs. The format in the marioexample2 needs to be followed.
Closing a window may be abit difficult but after a few clicks the window should close.